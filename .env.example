# Template only — never put real values here, this file IS committed.
# Real values go in .env.local (gitignored) and in the Vercel project settings.
#
# Server-only. No NEXT_PUBLIC_ prefix: these must never reach the browser bundle.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
